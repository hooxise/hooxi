rootProject.name = 'hooxi-config-server'

includeBuild('../hooxi-data-model')

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}